<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <title>Editar Documento</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 text-gray-800">
  <div class="max-w-2xl mx-auto py-10">
    <h1 class="text-3xl font-bold mb-6">Editar Documento</h1>

    <form action="/edit-document/<%= document.id %>" method="POST" class="bg-white p-6 rounded shadow">
      <div class="mb-4">
        <label for="title" class="block font-medium mb-1">Título</label>
        <input
          type="text"
          name="title"
          id="title"
          value="<%= document.title %>"
          class="w-full border border-gray-300 rounded px-3 py-2"
          required
        />
      </div>

      <div class="mb-4">
        <label for="content" class="block font-medium mb-1">Conteúdo</label>
        <textarea
          name="content"
          id="content"
          rows="8"
          class="w-full border border-gray-300 rounded px-3 py-2"
          required
        ><%= document.content %></textarea>
      </div>

      <div class="flex items-center justify-between">
        <button
          type="submit"
          class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700"
        >
          Salvar Alterações
        </button>
        <a
          href="/meus-documentos"
          class="text-blue-600 hover:underline text-sm"
        >
          ← Voltar
        </a>
      </div>
    </form>
  </div>
</body>
</html>
